<div class="design-holder">
<p><strong data-toggle="tooltip" title="Methods should not accept more than four parameters."> Parameter Overload -</strong> You have <%= @analysis[:ExcessiveMethodParams].length %> methods with excessive arguments in your code: </p>
<ul>
  <% @analysis[:ExcessiveMethodParams].each do |name| %>
    <li>
      The class <em><%= name[:class] %></em> has the method <em><%= name[:method] %></em>, which takes <%= name[:arity] %> arguments.
    </li>
  <% end %>
</ul>
<p>This may suggest these methods are doing too much. Is there a way you can break them down? <a class="resource-link" href="http://www.skorks.com/2009/08/method-arguments-in-ruby/">If not, here's an article that looks at ways you can pass in parameters in ruby.</a>
</div>
